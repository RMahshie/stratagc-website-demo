# StrataGC Website Demo

Frontend-only React and Vite implementation of the StrataGC landing page.

## Development

```bash
npm install
npm run dev
```

## Deployment

```bash
npm run deploy
```

The deploy command builds the site and publishes `dist/` to the repository's `gh-pages` branch.
